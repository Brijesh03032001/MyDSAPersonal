SELECT
    re.contest_id,
    ROUND(COUNT(re.user_id) * 100.0 / (SELECT COUNT(*) FROM Users), 2) AS percentage
from Register re
join Users on re.user_id = Users.user_id

group by re.contest_id
order by  percentage DESC,
    re.contest_id ASC;

