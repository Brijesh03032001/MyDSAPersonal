# Write your MySQL query statement below
select customer_id , COUNT(vi.visit_id) as count_no_trans
-- vi.customer_id , COUNT(tr.transaction_id) as count_no_trans
from Visits vi
left join Transactions tr on vi.visit_id = tr.visit_id

where tr.transaction_id IS NULL
group by vi.customer_id